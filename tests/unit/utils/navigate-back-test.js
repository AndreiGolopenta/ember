import navigateBack from '../../../utils/navigate-back';
import { module, test } from 'qunit';

module('Unit | Utility | navigate back');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = navigateBack();
  assert.ok(result);
});
