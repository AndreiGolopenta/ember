import toggleListClass from '../../../utils/toggle-list-class';
import { module, test } from 'qunit';

module('Unit | Utility | toggle list class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toggleListClass();
  assert.ok(result);
});
