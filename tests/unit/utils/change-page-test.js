import changePage from '../../../utils/change-page';
import { module, test } from 'qunit';

module('Unit | Utility | change page');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = changePage();
  assert.ok(result);
});
